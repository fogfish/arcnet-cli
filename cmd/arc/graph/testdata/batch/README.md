# Patch bundle

Hand-written notes about this bundle. This file carries no front matter at
all, so it declares no patch manifest — `arc apply batch` passes it over and
counts it as `not_a_patch` (spec 020 FR-003). It is never a failure.
