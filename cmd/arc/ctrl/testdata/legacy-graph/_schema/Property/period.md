---
"@id": period
"@type": Property
aligned: arc:period
merge: immutable
role: meta
---
# period

A timeline node's own period code (YYYY or YYYY-MM), duplicated from its @id so a bare 4-digit yearly value always decodes as a YAML string rather than an integer.
